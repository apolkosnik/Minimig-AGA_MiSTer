# AP040X2 audit: implementation vs doc/ specs and AP040_IMPLEMENTATION_PLAN.md

Date: 2026-08-16, branch ap040x2, working tree (includes the uncommitted
X2.2 fetch-queue diff to ap040_core.v).  Read-only audit: RTL and tests
were compared against AP040_IMPLEMENTATION_PLAN.md and the manuals in
doc/ (MC68040UM/UMAD, M68000PRM); spec claims below were verified against
the extracted UM text, with WinUAE-oracle conventions respected where the
project has recorded them as authoritative.  Line numbers refer to the
working tree as of this date.

Overall judgment: the architecture-visible core (MOVEC map, frame
formats, SSW/writeback fields, RTE validation, MMU translation/walk/
PTEST/PFLUSH, FPU datatype and trap model) audits clean against the UM
to an unusual degree -- most of what the plan calls "central design
items" is verifiably right, with file:line evidence collected below.
The real defects cluster in four places: interrupt-hold retargeting,
the FPU pending-exception/FRESTORE lifecycle, the FMOVE-out enabled-trap
path, and the X2.1 secondary-lane byte-write/DQM contradiction.  The
plan document itself is stale in several load-bearing places (P1 most
seriously: its STATUS paragraph describes the opposite of the tree).

============================================================================
## 1. Bugs in the ACTIVE configuration
============================================================================

### 1.1 [BUG] irq_hold_lvl can retarget the hold to a never-qualified level

rtl/ap040/ap040_core.v:178-196.  Capture correctly requires
`irq_lvl > sr[10:8]`, but the 45c4eb4f downward-tracking branch is
unconditional: `else if (irq_hold_lvl > irq_lvl) irq_hold_lvl <= irq_lvl;`.
Inside a level-3 handler (mask=3), a level-5 request qualifies (hold=5);
the level-5 source is then cleared while a level-3 request stays asserted:
hold tracks 5 -> 3 and `irq_pend` fires a level-3 interrupt AT the mask.
UM 8.2.6/Table 8-3: only levels strictly above the mask may be taken.
This is exactly the multi-source shape Paula produces.  Fix shape: in the
downward branch, requalify -- `irq_hold_lvl <= (irq_lvl > sr[10:8]) ?
irq_lvl : 3'd0;` (keeps the mask-raise survival for constant pins that
test 136 covers).  Note the dead `rte_irq_*` wires (core.v:596-604)
replicate the same logic and would inherit the bug if ever revived.

### 1.2 [BUG] FMOVE FPn,<ea> never delivers enabled arithmetic exceptions

rtl/ap040/ap040_fpu.v: `exc_req` is asserted only in F_WB (line 1504) and
the store pipeline never reaches F_WB.  Every store completion asserts
`done` with no `fpsr[15:8] & fpcr[15:8]` check: X/S/D/NaN/inf exits in
F_SRC (567, 586, 621, 640, 673, 707), integer stores in F_PACKI (1652),
denormal S/D packing in F_PACKS (1743).  The core's S_FPU_GO checks
fpu_exc_req (ap040_core.v:3626) but it is never raised for r_op==7'h7F.
UM ch.9: FMOVE-out is an arithmetic instruction; enabled OPERR (integer
overflow), SNAN, OVFL, UNFL, INEX2 must trap post-instruction (format $3
for memory destinations, vectors 49-54).  The FPSR status/accrued bits
are set correctly but no trap is taken, and the next dispatch clears the
status byte (fpu.v:488).  The enabled-arithmetic store cases (E49-E54)
appear untested by the corpus slices run so far.

### 1.3 [BUG] Reset and FRESTORE-of-NULL leave FP0-FP7 with wrong contents

ap040_fpu.v:417-419 (reset zeroes fr_s/fr_e/fr_m -> registers read +0.0)
and 450-454 (`fp_reset` from FRESTORE NULL clears FPCR/FPSR/FPIAR but not
the data registers).  UM reset state: FP0-FP7 = nonsignaling NaN ($7FFF,
all-ones mantissa); FRESTORE of NULL returns the FPU to reset state.
Consequence: an OS initializing a task's FP context with FRESTORE #NULL
leaks the previous task's FP register values.

### 1.4 [BUG] FRESTORE neither reports nor clears a pending deferred exception

ap040_core.v:3184-3186: S_FREST1 waits for fpu_bg but ignores
fpu_pend_exc, which is cleared only at hardware reset (1598) or delivery.
A stale F1-deferred exception survives FRESTORE #NULL and fires
pre-instruction on the NEW context's first FPU instruction -- with FPIAR
already zeroed, so the handler cannot identify a faulting instruction.
On a real 040 every FP instruction except FSAVE reports the pending
exception and FRESTORE replaces pending state wholesale.  Together with
1.5 this makes pending-exception state unsound across context switches.

### 1.5 [DIVERGENCE] FSAVE traps a pending deferred exception instead of saving it

ap040_core.v:3152-3155: S_FSAVE1 delivers fpu_pend_exc as a trap.  The UM
makes FSAVE the ONE FP instruction that does not report a pending
exception -- it writes the exception state (BUSY frame) for the handler.
Here a context-switch FSAVE traps in scheduler context, and the FP
exception handler's own FSAVE then sees IDLE, not BUSY.  This is the
documented no-BUSY-frame gap (ap040_fpu.v:19-20, ap040_core.v:5341-5344);
recorded here because the plan's F1 section calls FSAVE merely a "sync
point" without recording this consequence.

### 1.6 [GAP, known-open, confirmed] Interrupt does not outrank privilege violation

ap040_core.v:1481-1485: go_priv raises vector 8 unconditionally; no
irq_pend check exists in S_DECODE.  Matches CODEX_HANDOFF.md:32-36 (fix
prototyped, backed out, to be re-landed with its directed test on ap040
first).  This is the cputest irq/all ANDSR.W round-1 failure.

### 1.7 [RISK] MOVEM restart hole: base register loaded mid-loop

ap040_core.v:2831-2835 (S_MOVEM_LD writes each loaded register
immediately; base An written only at the end, 2790-2793; u_rec rollback
unused for MOVEM).  `MOVEM.L (A0)+,A0-A1` (or control-mode with the base
early in the list): a fault on a later transfer restarts with A0 already
overwritten by loaded data -- the restart recomputes the EA from garbage.
The real 040 continues MOVEM via the format-$7 EA precisely because
restart is unsafe here (UM 8.2.1, 8.4.6.7).  Pure restart is
plan-sanctioned (X2.3), but this case silently corrupts rather than being
merely slow, and a demand-paged OS (the NetBSD milestone) can reach it.
Cheapest fix within the restart model: defer the base-register (and any
register that feeds the EA) update to commit, or roll it back via u_rec.

### 1.8 [DIVERGENCE] Trace vs interrupt priority: UM disagrees, and the two boundary paths disagree with each other

fetch_next (core.v:1447-1463) takes a pending interrupt BEFORE a pending
T1/T0 trace (the completed instruction's trace event is lost, T bits
merely survive in the stacked SR); go_pc (1493-1498) checks tr_t1 BEFORE
irq_pend.  UM Table 8-4 puts Trace (6) above Interrupt (8): trace stacks
first, interrupt on top.  The comment at 1441-1446 says the IRQ-first
ordering is corpus-visible, so this is not called a bug outright -- but
straight-line and taken-branch boundaries currently implement OPPOSITE
orderings, and the corpus may contain no simultaneous T1+IRQ case.
Directed test wanted before X2.3 rearranges boundaries.

============================================================================
## 2. X2.1 sdram32_ctrl (committed, pre-integration)
============================================================================

The deliverable-gate numbers are real: the bench was independently
rebuilt and re-run during this audit -- ALL TESTS PASSED, lockstep clean
on all 2018 checked cycles, fill latency 15 clk_114 dual / 31 fallback,
and all three deliberate breaks reproduce the commit's exact error counts
(2450 / 5214 / 536, incl. the 5a50beef signature).

### 2.1 [BUG] Secondary-lane byte writes depend on a DQM pin the io-board does not route

rtl/sdram32_ctrl.v:65-69 states the premise (SDRAM2 has no DQM;
sys/sys_dual_sdram.tcl:4-41 confirms -- no DQM, no CKE), and nCS
whole-slot masking correctly handles LANE granularity.  But byte writes
to an even word (addr[1]=0, secondary lane) still drive per-byte masks on
sd2_dqm (sdram32_ctrl.v:859, 878, 964), which physically goes nowhere.
With DQM strapped low (required for reads), every byte-granular write to
an even word writes BOTH bytes of the secondary chip, clobbering the
partner byte.  tb_sdram32.v:337 connects the phantom pin to the model, so
the gate's byte-write test (tb line 923, word 0x202 upper-byte) passes in
sim and cannot pass on hardware.  Needs read-modify-write for
secondary-lane byte writes (or evidence the board routes DQM) BEFORE
X2.1b integration.

### 2.2 [GAP] Runtime fallback probe absent

Plan X2.1: "runtime detectable: probe pattern on SDRAM2 at init; config
error out."  Nothing exists; DUAL_SDRAM is compile-time only
(sdram32_ctrl.v:124-128).  A DUAL build on a single-module board would
silently read a floating upper lane.

### 2.3 [GAP] tb_sdram32 not in run_tests.sh; break modes not regression-runnable

The three deliberate breaks were one-off manual edits recorded only in
the commit message -- no plusargs/parameters exist to re-run them.
run_tests.sh has no tb_sdram32 leg, so X2.8's "green at every commit"
does not cover the new controller.  Recommend plusarg-selectable break
modes and a run_tests.sh leg.

### 2.4 [DIVERGENCE] Command engine copied, not instantiated

Plan says "instantiating the proven sdram_ctrl command engine once";
sdram32_ctrl.v is a forked copy (header admits it, lines 31-37) and
sdram_ctrl.v stays in the build -- every future sdram_ctrl fix must be
mirrored by hand.  The diff against the original confirms slot machine,
refresh, write buffer and walker discipline are line-for-line intact;
deltas are exactly the documented ones.

### 2.5 [RISK] Dual-read second CAS shrinks turnaround/precharge margin

Dual chip/cache read slots issue READ2-with-autoprecharge two states
after READ1 (sdram32_ctrl.v:932-944): the discarded 4th beat lands at the
next slot boundary, DQ release ~1 state (~8.7 ns) before a next-slot
write drives (vs 3 states in sdram_ctrl), and internal precharge starts
2 sd_clk later (tRP = 2 sd_clk = 35 ns -- fine for datasheet parts, one
cycle less margin than proven silicon behavior).  The behavioral model
checks neither tHZ nor tRP, so only hardware bring-up can see this.

### 2.6 [RISK] "Width doubled" holds for reads/fills only

The new 32-bit port is read-only line fill (fill_*, sdram32_ctrl.v:193-
201); cpuWR and the write buffer stay 16-bit (a 32-bit store = two
lane-masked slots).  Fine for the T2 gate; the X2.7 internal-cache
replacement gets no store-bandwidth help from X2.1 as it stands.

Verified OK: lockstep structural copies + every-cycle tb identity check;
chipset lane routing by addr[1] for reads AND writes with cycle-identity
against sdram_ctrl on shared stimulus; nCS masking spans ACTIVE through
precharge and exists only in the two 16-bit write arms (reads, refresh,
init never mask -- both chips get AUTO REFRESH and mode-register loads);
single clock domain, no new CDC; walker/snoop ports intact and
eq-checked; 15/31 clk_114 latencies consistent with the state machine
(the plan's "8 clk_114 impossible" floor argument is arithmetically
correct).  X2.1b integration confirmed not started (Minimig.sv:425 still
instantiates sdram_ctrl; no qsf/qip references) -- as the commit states.

============================================================================
## 3. X2.2 fetch queue (uncommitted working-tree diff)
============================================================================

The 437-line diff is a complete implementation of the planned queue: the
epf ring (4 longwords) becomes free-running, filled by a fill engine
whenever the port is idle (ap040_core.v:~5445-5505), drained by
S_FETCH/S_IMMF as pure poppers with a same-cycle bus-forward path;
longword immediates consumed in one pass; flushes on exception entry,
interrupt recognition, MOVEC/PTEST/PFLUSH/CINV/STOP/RESET/HALT, redirect
mismatch, and S-bit change; prefetch is page-guarded (`epf_ftail[31:12]
== pc[31:12]`, conservative for 8K pages, so safe); speculative-fetch
faults recorded in epf_err and re-taken only at the demand point;
killed-fetch faults discarded.  The working tree is suite-green (all
build/*.log ALL TESTS PASSED) and 32 cputest flow-control slices pass
with 0 mismatches.  tb_dat_replay.v's uncommitted change is the matching
harness fix (inject_state clears epf_count/epf_armed).  Exposures:

### 3.1 [RISK] Locked-RMW indivisibility broken at the core/adapter boundary

The fill engine's exclusion list (~5498-5500) does not test lk_cyc
(core.v:627).  CAS/CAS2/TAS intermediate compare states are neither
excluded nor port-idle-free, so a background fetch can be issued between
the locked read and locked write: the bus sees R(locked)-FETCH-W(locked),
violating plan section 8's "locked transfers remain indivisible".  Low
practical impact on this fabric (no agent honors CPU-port lock), but it
is a new contract deviation; adding `!lk_cyc` to the self-fill condition
restores it.

### 3.2 [RISK] Self-modifying-code coherence of the queue -- the fetchbuffer failure class

The queue runs up to 16 bytes ahead continuously; no path compares store
addresses against epf_next..epf_ftail and stores do not flush.  Strictly
040-accurate (SMC requires CPUSH/CINV, and CINV flushes the queue), but
rtl/ap040/experimental/README records the previous fetch-buffered core
"booted DiagROM but not AmigaOS on hardware (sim boots both)" -- exactly
this hazard class, invisible to the suite (t_cache's SMC test uses CINV).
Hardware validation is the only closure; a queue-vs-store address snoop
would be cheap insurance.

### 3.3 [GAP] X2.8 discipline not yet met for the diff

No directed tests seen-to-fail cover the new deferral (epf_err), discard
(epf_kill), or data-op-vs-pending-fetch hold paths (epf_err is reachable
essentially only via berr injection on a speculative fetch, since the
page guard rules out translation differences).  No occupancy measurement
exists: the X2.2 gate is S_FETCH+S_IMMF < 20% on t_integer, cycle claims
must come from the tb_prof histogram, and no tb_prof is in the tree.

### 3.4 [RISK] The occupancy gate can read as met while wall clock does not move

cpu_wrapper.v:267 (`clkena_in = ~cpu_req | bus_complete | bus_berr`) and
the ce-gated core block mean any background fetch stalls the WHOLE core
until it completes: bus wait migrates out of S_FETCH/S_IMMF into whatever
state was running.  Until the fetch port decouples onto the X2.1 32-bit
bus, measure wall-clock cycles alongside occupancy.

### 3.5 [RISK] exception_prefetch assumes no pending queue fetch, two states from its enforcement

core.v:~1207-1225 issues the first vector-stream word unconditionally;
today unreachable with epf_pend set (S_EXC0 waits at ~2436), but the
invariant is remote from its enforcement.  An assertion or explicit pend
check would survive future resequencing.

============================================================================
## 4. Plan document staleness (plan says X, tree does not-X)
============================================================================

### 4.1 P1 STATUS is inverted -- the most misleading text in the plan

Plan lines 669-671 claim "DONE and enabled.  ap040_cache is the only
cache in the system; both cpu_cache_new instances are built without their
storage (CPU_CACHE 0)".  The tree: cpu_wrapper.v:258 sets
AP040_ENABLE_CACHE(0) ("Internal caches OFF: they do not fit this device
with usable timing", 248-257); Minimig.sv:425 and :477 instantiate
sdram_ctrl/ddram_ctrl with CPU_CACHE(1); Minimig.sv:418-424 documents the
revert (measured slower; snoop CDC loses events while clkena frozen).
X2.7/X2.8 already reflect the revert -- only the P1 paragraph was never
corrected.  P1's fit table (internal cache 609 ALMs SMALLER) and the
cpu_wrapper comment (over capacity, -0.716 ns) also flatly contradict
each other; per the X2.8 evidence rule the revert is the trusted state.

### 4.2 P1's "no snoop port" consequence paragraph is stale the other way

Plan 699-704 says ap040_cache has no snoop port so chip RAM is uncached.
RTL: the snoop port exists (ap040_cache.v:62-68), chip RAM is in the
cacheable windows because of it (ap040_tg68k_compat.v:252-261), and
sdram_ctrl exports snoop_tgl/snoop_addr (sdram_ctrl.v:66-71,164-176)
crossed in cpu_wrapper.v:230-245.

### 4.3 F0 text understates the shipped divide/sqrt

Plan F0 says 2 bits/digits per cycle (~41/~40 cycles); the RTL is 3 per
cycle (ap040_fpu.v:1291-1333, 1335-1366 -- the "follow-up headroom" item
already done; X2.5's preamble numbers match the RTL, F0's do not).  The
F_MULT half of F0 is exact as written (single registered 64x64 product,
fpu.v:1286).

### 4.4 Sections 1/4/5 wrapper text superseded by the AP040-only baseline

No CPU mux exists (cpu_wrapper.v:177-200); ap040_tg68k_compat is the only
CPU; fx68k/tg68k removed at 8a0c72b4 by design.  Nothing dangles, but
cpucfg[0] now has NO reader anywhere -- a dead OSD bit (former 68000/010
select).  cpucfg[1] retains meaning (turbochip/turbokick, autoconfig,
ide_fast, cpu_type for ram_cs_guard).  Planned sideband left open at the
wrapper: .fc(), mmu_addr_log/phys, cache burst ports
(cpu_wrapper.v:289-312) -- consistent with the walker-channel
architecture; the section-5 signal list is stale.  Section 4's port list
is implemented with benign extensions (parameters, snoop/window ports,
nmi_ack_toggle, cache_maint_*).

### 4.5 Section 11 continuation text superseded by X2.3 (documented, consistent)

The $7 generator never sets CP/CU/CT/CM (aer_ssw, core.v:1393-1402) and
RTE $7 pops 60 bytes without reading them (2568-2574) -- consistent with
X2.3's "format $7 stays synthetic".  A handler that SETS a continuation
bit is silently ignored; acceptable until X2.6 revisits real writebacks.

### 4.6 Format $4 recognized, never generated

RTE accepts $4 only when AP040_HAS_FPU==0 (core.v:2557-2566, correct both
ways) and the stacking path exists, but no exc() call passes fmt 4: with
FPU disabled, F-line takes vector 11 format $0 (5317-5318) instead of the
LC040 $4 frame (EA + faulted PC) plan section 12 stage 1 requires.  Only
matters for a future LC040 build config; the core header's "$4" claim
overstates.

### 4.7 Section 13's copyback tests: feature deliberately absent

Write-through-with-invalidate only; copyback deferred by plan 385-387 and
X2.7 re-plans it on the 32-bit bus.  Stale as a checklist item, not a
test omission.

============================================================================
## 5. Latent bugs in compiled-out code (internal cache snoop paths)
============================================================================

All latent because AP040_ENABLE_CACHE=0, but cpu_wrapper.v:257 invites
flipping it back on; fix before anyone accepts that invitation.  These
are precisely the paths with zero test coverage: no bench drives
cache_snoop_stb/s_stb nonzero (tb_ap040_program.v:81 ties it 0).

- 5.1 [BUG] Snoop strobe lost while clkena frozen: snoop_stb_r is a
  single free-running-clk pulse (cpu_wrapper.v:234-245) but the cache
  acts only on ce cycles (ap040_cache.v:122, 309); a chipset write
  landing during a chip-bus wait never invalidates.  Admitted in
  Minimig.sv:422-423.
- 5.2 [BUG] Port-A/B tag-RAM races: a snoop in the last fill-beat cycle
  is undone by C_TAGW's rebuilt valid bits (bram.vhd mixed-port RDW is
  DONT_CARE; ap040_cache.v:255-265); a snoop in the C_TAGW cycle is a
  same-row write/write collision (262 vs 277); a snoop in the rd_accept
  cycle can let C_LOOK hit the just-killed line (252-253, 378-385).
  Same hazard class as the fixed cpu_cache_new fill-tag regression.
- 5.3 [BUG] Port-B arbiter can drop a store invalidate: line-crossing
  store accepted while a snoop wins port B -- the replay cycle serves
  store_inv_lost while winv_pend is cleared as if issued
  (ap040_cache.v:313-320, 329-343, 361-366); the second line's
  invalidate never issues -> stale line after a write-through store.
  store_inv_lost is also a single overwritable entry.
- 5.4 [RISK] Line fill has no berr handling: on berr the adapter aborts
  without mem_ack, the cache re-issues forever while the core has taken
  mem_err and moved on (ap040_cache.v:394-403,
  ap040_bus16_adapter.v:118-131) -- livelock instead of a format $7.
  Unreachable while fills are window-gated to always-acking fast RAM;
  latent for the X2 32-bit fill path.
- 5.5 [RISK] Snoop source coverage if re-enabled: ddram_ctrl exports no
  snoop, so walker U/M writes to page tables in Z3 would not invalidate
  internal D-lines caching descriptors; snoops/stores invalidate only
  the data bank (inv_idx MSB tied 0), so DMA-loaded code leaves stale
  I-lines until CINVA (matches Amiga discipline, weaker than a real
  snooping 040).

Verified OK (cache): CACR is the 040 layout, masked $8000_8000, IE/DE
wired right way round at both consumers; CINV/CPUSH decode (F4xx, cache
field per PRM, scope 00 -> F-line before privilege, supervisor check,
prefetch-queue flush) with the bounded 128-row sweep handshake;
organization is plan-level-2 exactly (split I/D, 4KB, 64x4x16, physical
tags, write-through); reset sweep before serving; MMU/TTR CM ->
c_nocache and mmu_cache_inhibit into all three controllers; the current
build's coherence story is sound (chipset + walker snoop into
cpu_cache_new, the 6f2310e7/959c11d7 fill-tag fix present with its
regression bench); invalidate-on-write instead of update-on-write is
spec-divergent but safe (memory always current); CINV/CPUSH scope
collapse to ALL is safe ONLY while write-through -- it becomes data loss
the day copyback lands.  External-cache CINV completing before the clear
lands (cinv_done tied 1, maintenance toggle accepted cycles later) is
the field-proven legacy TG68K contract, noted not flagged.

============================================================================
## 6. MMU: clean, with two walker-CDC risks that gate X2 work
============================================================================

No outright spec [BUG] in translation, walk, protection, PTEST/PFLUSH or
fault logic.  Architecture: ONE shared module, but per-side resources
match the UM -- banked 128-entry array = a 64-entry 4-way 16-set ATC per
side (ap040_mmu.v:123-140), ITT/DTT split by c_instr, one shared walker
datapath (what X2.7 describes).  Set indexing/tagging match real silicon
exactly (4K: LA[15:12] index, LA16 in tag; 8K: LA[16:13], PA12<=LA12).

### 6.1 [RISK] Walker-CDC ack/ce invariant -- X2.2/X2.4 will break it

ap040_walker_cdc.v:86-91 produces s_ack as a single s_clk pulse; the MMU
samples it only under ce (ap040_mmu.v:336, 227; compat wires
.ce(clkena_in)).  Safe today only because walks happen while the CPU bus
is idle, so ce=1 throughout -- an unstated cross-module invariant.  A
decoupled fetch front end (bus busy during a data-side walk) makes ce=0
cycles possible during a walk: a missed ack permanently hangs the walker.
Must be addressed (latch the ack, or clock the MMU walker interface
free-running) before X2.2's port decoupling or X2.3.

### 6.2 [RISK] Walker-CDC reset asymmetry can desync toggle parity

s_reset_n(cpu_rst) vs m_reset_n(~reset_d) (Minimig.sv:392, 404).  A
CPU-only reset during an in-flight walk can leave parity desynced
(walker_cdc.v:38-52): phantom replay of a stale request (including a
stale descriptor WRITE) or an instant false ack with stale data to the
first post-reboot walk.  Narrow window, silent corruption.

Deliberate simplifications, judged benign: invalid translations never
cached (stricter than UM 3-14/15 -- costs a re-walk per retry of a
nonresident page, buys PFLUSH-free descriptor fixes); U/M writeback is
read-then-write, not locked RMW (single-CPU-master argument holds;
pass_ok blocks all CPU traffic for the whole walk; only DMA into page
tables could lose an update); PTESTW on a write-protected TTR returns
B ($0800) per WinUAE instead of the UM's $0003 -- oracle-sanctioned;
replacement counter increments per fill, not per access (policy is
implementation-defined in effect); U1/U0 cached and reported but drive
nothing (no UPA pins exist in this SoC).

Verified OK highlights: TTR field semantics and TTR0 priority; E-bit
independence; identity default translation; three-level walk index
extraction per UM Fig 3-11 for both page sizes; UDT/PDT decode incl.
indirect-once; WP accumulation; U set on touch, M on write with the
UM 3-13 suppression rule; M-miss re-walk; one-shot fault handshake into
format $7 vector 2 with SSW ATC/MA/LK/RW/SIZE/TT/TM correct; PTEST
forced-walk + MMUSR layout exact per UM Fig 3-6 (incl. LA12->PA12 for
8K); PFLUSH all four variants with G-bit honor and privilege; MMUSR
32-bit at $805; TC/TTR/URP/SRP write masks exact; warm reset clears only
the five E bits and does NOT flush ATCs (UM 3.6.1/3.7.4); walker channel
transfers exactly one transaction per toggle; table-search writes cannot
interleave with CPU writes (pass_ok + epf_pend gates); cache-mode routing
to both the internal cache and all three controllers, with walker writes
snooped so cached descriptors stay coherent.

============================================================================
## 7. Core exception model: remaining notes
============================================================================

- 7.1 [RISK] Double-fault window broader than UM 8.2.1's narrow list:
  in_exc is set for EVERY exception at S_EXC0 (core.v:2443) so a fault
  while stacking a TRAP/interrupt frame, fetching its vector, or
  prefetching its handler halts, where the narrow reading wants a
  recoverable access error.  The UM contradicts itself (7.6.3/1.4 use
  broad wording; 8-7 demands a resident supervisor stack precisely so
  this never happens), so the halting reading is defensible; unreachable
  under a sane OS.  Same for RTE frame-validation faults halting vs UM
  8.4.6.7's frame-below-frame description.
- 7.2 [RISK] RTE to an odd PC stacks the RESTORED SR without forcing S
  (S_RTE_FIN2 core.v:2587-2593); UM 8.4: the address-error frame's SR
  has S set.  The corpus validated popped-SR stacking, but possibly
  never with a USER SR image; directed check recommended.
- 7.3 [RISK] Dead code from the reverted RTE-deferral experiment:
  rte_irq_pend/rte_irq_take_lvl computed and never used (596-604),
  rte_oldsr written and never read -- and the unused wires replicate the
  finding-1.1 hold logic.  Delete or fix in place.
- 7.4 [RISK] t0_special includes TAS (core.v:1058); the UM p.8-9/10 T0
  change-of-flow list does not.  WinUAE-derived, possibly
  hardware-true; only matters if the trace corpus lacks TAS-under-T0.

Verified OK highlights: MOVEC map exact per PRM 6-22/23 (CAAR correctly
absent, unknown codes -> illegal, write masks exact, RSTI preserves MMU
regs except E bits); every frame format's length/fields/vector
assignments per the UM tables, incl. the $2 "address = branch target
with bit 0 cleared" address-error convention and $7 at A7-60 with
$7008; RTE validation/pop lengths/throwaway re-fetch/format-error frame;
M-bit interrupt entry with ISP throwaway (oracle-validated where the UM
self-contradicts); NMI edge semantics with two-sample sync; pending-IRQ
preemption at handler entry per UM 8.3; trace T1/T0 with the 040
pipeline-sync list, 4AFC cancellation, traced-STOP; double-fault
quiescence and terminal S_HALT; USP/ISP/MSP banking with collision-free
aux port; complete privilege-check battery with EA-legality-first
ordering where the family requires it; restart machinery honors plan
section 11 (per-page split cycles, u_rec rollback, epf deferral/discard)
except the MOVEM hole (1.7).

============================================================================
## 8. Test coverage vs plan section 13 / X2.8
============================================================================

Strong on the directed side; the structural gaps:

- tb_sdram32 (X2.1 gate), the cputest dat-replay runner (X2.8 calls it
  THE critical item), tb_ap040_diagrom, and tb_cpu_wrapper_chip_bridge
  are all standalone -- not in run_tests.sh, so the per-commit gate does
  not cover them.  dat-replay itself is healthy and actively used
  (fresh passing shards in build/cputest/).
- MISSING directed tests: unconsumed-prefetch fault deferral and
  branch-discard (newly load-bearing under the X2.2 queue -- needs tb
  berr injection, see 3.3); translated exception stacking that forces a
  supervisor-stack page walk; walk berr during a NORMAL access (only
  the PTEST->MMUSR B shape is covered).
- PARTIAL: CINV/CPUSH/PTEST/PFLUSH user-mode privilege traps (only
  MOVE-to-SR priv is tested); PFLUSHN nonglobal variant; format $3
  frame word directly checked (only $2 words are); exhaustive bus16
  byte/word/misaligned matrix (gap discipline and page-split are
  covered; the full size matrix is not).
- N/A this config: format $4 (needs an AP040_HAS_FPU=0 build), copyback
  (feature absent by plan).

Coverage that exists and where: reset (tb_ap040_reset); frames $0/$1/$2
+ $7 + invalid-RTE + M-bit/throwaway + MOVEC matrix + MOVES + decode
legality (asm/t_exceptions.s); MOVE16 functional + line-fault SSW/WB3 +
restart (t_integer.s:468-485, t_mmu.s:537-558); data fault restart
read/write/TAS-RMW/MOVEM/MOVE16 (t_mmu.s:159,174,562,398,537); history
bits (t_mmu.s:112-129); PTEST/MMUSR incl. TC-off (130-158, 517-530);
page PFLUSH + pflusha (385-397); cache enable/SMC/DMA-stale/inhibit/
disable (t_cache.s, run against AP040_ENABLE_CACHE(1) benches); FPU
arithmetic/FSAVE/FRESTORE/traps/IRQ-soak (t_fpu.s, 4654 lines);
F1 overlap + deferred delivery (t_fpu 285-289); withdrawn/qualifying
interrupt windows (t_exceptions 135/136).

============================================================================
## 9. FPU: remaining notes (beyond 1.2-1.5)
============================================================================

- 9.1 [RISK] Two delivery timings for the same enabled exception:
  special-operand paths (NaN/inf/zero fast paths, FCMP/FTST SNAN) never
  pass an `accepted` state, so they deliver immediately with stacked
  PC = next instruction (core.v:3626-3629), while numeric paths defer to
  the next FPU dispatch with stacked PC = that instruction (3244-3251).
  A real 040 defers both.  Pre-F1 baseline behavior, corpus-validated;
  flagged as a consistency risk for FPSP-style handlers.
- 9.2 [GAP] FRESTORE validates full 32-bit header longwords ($41000000/
  $41300000) rather than the version/format bytes (core.v:3193-3205):
  a real-silicon IDLE frame with a nonzero undefined low half-word, or
  any BUSY frame, format-errors.  Documented in both file headers.

Verified OK highlights: F0 multiply exactly as claimed (registered 64x64
DSP product, 2-state F_MULT); F1 `accepted` export placed after every
unimp/unsupp decision incl. the late F_EXEC/F_BIN destination checks;
scoreboard gate list complete (S_FPU_DEC, S_FBCC, S_FSCC0 covering
FScc/FDBcc/FTRAPcc, S_FSAVE1, S_FREST1, S_EXC0) and every FPU-register
consumer structurally routes through the gated S_FPU_DEC; deferred
delivery pre-instruction with FPIAR naming the faulting op; hardware
instruction subset exactly the 040's (FSGLMUL/FSGLDIV included,
transcendentals/FMOVECR/FREM/FMOD/FSCALE trap v11 with rev-$41 UNIMP
state and FPSP-shaped $2 frames; $78-$7F -> vector 4 per WinUAE);
unsupported-datatype rules exact incl. the pseudo-denormal
integer-bit-set rule; FPCR/FPSR masks, per-instruction status clear,
accrued mapping, vector priority, cc rules, BSUN enabled/disabled paths,
FPIAR write set (FScc/FDBcc/FTRAPcc yes, FBcc no); FSAVE/FRESTORE
mechanics (EA legality, supervisor check order, -(An)/(An)+ adjust,
NULL restore of control regs); rounding/precision structure incl. FSGL
semantics and reserved-precision-as-double; X2.5 confirmed not started
(one FSM, one op in flight, fpu_bg overlap only) -- as planned.

============================================================================
## 10. Suggested order of work
============================================================================

(Executed 2026-08-16/17 -- every item below was either fixed or
formally dispositioned; section 11 has the outcomes and the remaining
open list at its end.)

1. Fix 1.1 (irq_hold_lvl requalification) with a directed test that
   fails on the current RTL (multi-source downgrade window), alongside
   re-landing the known 1.6 priority fix -- same area, same test file.
2. Fix the FPU lifecycle cluster 1.2/1.3/1.4 (and decide 1.5's
   documented behavior is acceptable until a BUSY frame exists); these
   are the "validated before advertising full 040 mode" items from plan
   section 12.
3. Resolve 2.1 (secondary-lane byte-write DQM) before X2.1b -- RMW in
   the controller or board evidence; add 2.2's probe and 2.3's
   regression-runnable breaks + run_tests.sh leg while in the file.
4. For the X2.2 diff before commit: add !lk_cyc to the self-fill
   condition (3.1), build tb_prof and take the occupancy + wall-clock
   measurement (3.3/3.4), and write the berr-injection deferral/discard
   directed tests (3.3).  Decide 3.2 (store snoop) after hardware.
5. Address the walker-CDC pair (6.1/6.2) before X2.2 port decoupling or
   X2.3 -- 6.1 is a hard prerequisite for any overlap of fetch and
   walks.
6. Correct the stale plan text (4.1/4.2/4.3 at minimum) so the next
   session does not act on P1's inverted STATUS.
7. The latent cache items (section 5) block only the re-enable
   experiment; fix 5.1-5.3 with a directed s_stb bench first if that
   experiment returns.

============================================================================
## 11. Fixes applied same day (2026-08-16, uncommitted with the X2.2 diff)
============================================================================

1.1 FIXED.  ap040_core.v irq_hold_lvl downward branch now requalifies
against the mask (irq_lvl > sr[10:8] ? irq_lvl : 0).  Directed coverage:
t_exceptions test 137 -- tb_ap040_program's new $F150 IPLSTEP device
models two devices sharing the encoder (level A withdraws after N
cycles, level B keeps requesting); the sweep was seen to fail on the
pre-fix RTL at 7 widths in both phases (level-3 accepted at mask 3) and
passes post-fix.  The tb also gained a shadow-IPEND reference model
(tb_qual) enforcing the mask invariant on EVERY interrupt acceptance in
every test: at/below-mask acceptance is legal only on a surviving
qualified claim (test 136's IPEND case), anything else fails the run.

3.1 FIXED.  The X2.2 fill engine no longer issues speculative fetches
while lk_cyc is set, so TAS/CAS/CAS2 read->write stays indivisible on
the bus.  Directed coverage: tb_ap040_program's lk_window monitor
(fetch bus cycle between a completed locked read and the locked write
fails the run) -- seen to fail 6 times on the pre-fix RTL in t_integer's
existing CAS/TAS tests.  First fix attempt (blocking ALL fills under
lk_cyc) hung the CAS dat-replay slices at S_IMMF: lk_cyc is set at
decode, before the extension words are consumed, and S_IMMF depends on
the fill engine when the queue is dry (state injection empties it).
Final form allows the fill only for the S_IMMF demand; those fetches
precede the locked read, so the contract holds -- monitor green.

3.1b FIXED (found by the new monitor, not in the original audit).
lk_cyc was cleared only by fetch_next, which is not on the exception
entry path, so a FAULTED TAS/CAS leaked lk_cyc=1 into its access-error
handler: handler data reads armed the monitor (t_mmu's TAS
operand-fault test failed on it), and the stale flag throttled the
handler's fetch queue.  exc() and aerr_start now clear lk_cyc -- the
040 drops LOCK at the fault; aer_lk still captures the pre-edge value
for the SSW.

Verification state after all three fixes: run_tests.sh green
end-to-end; corpus slices CAS* 25/25, TAS* 7/7, IRQ+ODD_IRQ 100/100,
all with 0 mismatches and 0 harness errors.

Verification state after the FPU round (1.2/1.3/1.4): run_tests.sh
green end-to-end (t_fpu now 306 tests across all platform legs); the
representative cputest gate 6/6; FMOV* slices 19/23 with the 4
failures pre-existing on HEAD (see the FMOVE.P note below).

1.3 FIXED.  Reset and FRESTORE-of-NULL now put the default
nonsignaling NaN (positive, exponent $7FFF, mantissa all ones -- WinUAE
fpu_reset/fpnan/fpu_null, xhex_nan) in FP0-FP7 instead of +0.0.
Directed coverage: t_fpu 292-294 store fp7 before anything touches the
register file (seen failing pre-fix in all phases), 296-297 check fp2
after FRESTORE #NULL.

1.4 FIXED.  A completed FRESTORE (NULL, IDLE, or UNIMP frame) now
discards a pending deferred exception with the rest of the old
context's state.  On silicon the pending state lives inside the FPU and
FRESTORE overwrites it wholesale; WinUAE's fpuop_restore confirms
FRESTORE is not a reporting instruction (it never calls
fp_exception_pending -- only arithmetic/FBcc/FScc/FDBcc/FTRAPcc do).
Directed coverage: t_fpu 295 arms a deferred SNAN, FRESTOREs NULL, and
requires the following FNOP to deliver nothing (seen failing pre-fix
after 1.3 landed).

1.2 FIXED.  FMOVE FPn,<ea> now delivers ENABLED arithmetic exceptions.
Store exits in ap040_fpu funnel through a new F_STDONE completion state
(one cycle later, after the status byte settles -- the F_WB pattern)
which raises exc_req with the priority vector when fpsr&fpcr enables
are nonzero.  Core-side delivery per the WinUAE oracle: integer-store
SNAN/OPERR suppresses the destination write and traps post-instruction
(fault_if_68040_integer_nonmaskable returns before the store);
float-format and Dn stores write the default result first, then trap
post-instruction; frames are format $3 with stacked PC = next opcode
and EA = destination (0 for Dn), matching the existing v55 store
shape.  Disabled exceptions keep completing in hardware with the
architectural default results (AP040's documented no-FPSP design; the
corpus excludes the 040 nonmaskable set at generation, so this stays
corpus-neutral).  Directed coverage: t_fpu 298-300 (integer store
suppressed + EA), 301-303 (Dn suppressed + EA=0), 304-306 (single
store: +inf written, then v53).  Negative control: with F_STDONE's
enable check forced off, 298 fails in all phases; restored.

Not fixed, unchanged by design for now: 1.5/finding 6 (FSAVE traps a
pending exception instead of writing a BUSY frame; no BUSY frame
exists) and finding 9.1 (special-operand register ops deliver
immediately where numeric ops defer) -- both documented in the file
headers.

3.3/3.4 CLOSED (2026-08-17).  tb_ap040_program gained +prof -- the
tb_prof state histogram X2.8's cycle-claim rule demands (per-state
cycles with a clkena-stall column, dumped per phase).  Measured on
t_integer phase 0: 17236 cycles (20106 after the peephole round,
23750 at baseline; -27% cumulative, -14% from the queue), occupancy
S_FETCH+S_IMMF 51% of which only 1799 cycles are bus-wait stalls.
The <20% occupancy gate proved STRUCTURALLY unreachable by any queue:
decode folds into S_FETCH's pop cycle, so one fetch cycle per
instruction plus one immediate cycle per extension word (~41% of the
total, active) are irreducible in the multi-cycle FSM -- the gate was
a spec error of the X2.1 "8 clk_114" kind and has been re-scoped in
the plan (wall clock + fetch-stall reduction for X2.2; the occupancy
line moves to X2.3, whose definition is overlapping fetch with
execute).  The queue's fault paths got their directed tests via a new
$F154 one-shot FETCH bus error in the tb (the only reachable
speculative-fault source given the page guard): t_exceptions 138
(speculative fault discarded on redirect), 139 (deferred fault
re-issued at demand, consumed one-shot, clean continuation), 141
(demand fetch fault: format $7, FA = fetch address, supervisor
program space, RTE restart).  Negative control: with the fill engine
forced to take speculative faults immediately, 138 fails in every
phase; restored.

2.2 FIXED and X2.1b IMPLEMENTED (2026-08-17).  sdram32_ctrl gained the
runtime module probe: init extends by 8 slots (DUAL builds only; the
DUAL=0 init is cycle-identical to before) doing two write/read pairs
through unit 0 with complementary per-lane patterns -- byte masks
travel on the A12/A11 short, the primary is checked with the
complement (also catching crossed lanes), and dual_ok latches only if
both patterns return on both lanes.  tb_sdram32 asserts dual_ok on
the normal run, ties it on the DUAL=0 build, and the new +no_module
mode (secondary model never drives its DQ, like an empty socket)
requires the probe to refuse -- wired into run_tests.sh as the
sdram32_nomod leg.  Integration: Minimig.sv instantiates sdram32_ctrl
behind MISTER_DUAL_SDRAM (framework emu ports already exist in
sys/emu_ports.vh), SDRAM2 outputs Z-gated on SDRAM2_EN, and
dual_fault (= ~SDRAM2_EN | ~dual_ok) holds minimig's rst_ext so a
DUAL build without its second module refuses to run instead of
silently losing half of every chip-RAM longword; rtl/ram.qip carries
sdram32_ctrl.v, and Minimig.qsf documents the two-line variant recipe
(macro + sys_dual_sdram.tcl in place of sys_analog.tcl -- the variant
has no VGA by framework rule).

1.5 TIER 1 IMPLEMENTED (2026-08-17).  FSAVE now EXTRACTS an e1-class
pending exception as the $41/$30 frame instead of trapping, per the
WinUAE recipe: at a released op's deferred-exception retire the core
pulses pend_capture and the FPU prepares the frame from an operand
shadow taken at F_EXEC/F_BIN entry (cmdreg1b via frame_cmd1, ETEMP =
the source, stag, E1 flag, GRS=7/WBTE15 for SNAN, GRS=1 otherwise; the
frame writer and restore parser gained the GRS and WBTE15 fields,
zero on the unimp path).  S_FSAVE1 waits out the one-cycle preparation
(a same-edge race found by test 307) and only the e3 class --
OVFL/UNFL/INEX from the five arithmetic ops, whose BUSY frame is
Tier 2 -- still traps, now explicitly guarded by test 318.  FRESTORE
of an E1 frame whose command the hardware implements re-arms the pend
(vector recomputed from the restored FPSR/FPCR enables), delivered
pre-instruction at the next dispatch; with enables cleared the state
executes through, and a delivered-but-unsaved frame lingers so an
FPSP-style handler's first-instruction FSAVE still reads it.
Directed coverage: t_fpu 307-318 (no trap at FSAVE, frame id/E1/
CMDREG1B/GRS/WBTE15/ETEMP contents, FRESTORE re-arm delivery with
inhibited writeback, enables-cleared execute-through, e3 trap guard);
negative control (S_FSAVE1 forced back to always-trap) fails 307 in
every phase.  Also found by the tests: a last-assignment-wins bug in
my own pendcap pulse ordering, and the S_FSAVE1 preparation race.

1.5 TIER 2 IMPLEMENTED (2026-08-18) -- finding 6 and the last FSAVE
gap closed.  A pending OVFL/UNFL/INEX from the five arithmetic ops now
extracts as the true 100-byte $41/$60 BUSY frame per WinUAE's 68040
recipe: E3 flag, swizzled CMDREG3B (the existing frame_cmd3), WBTEMP =
the FPU's internal intermediate captured at F_ROUND's range decision
(rounded {sign, er, mr} for OVFL/INEX, unrounded for UNFL, with the
live GRS bits), FPIARCU, source ETEMP/STAG, and the destination
FPTEMP/DTAG for dyadic ops (shadowed at F_BIN entry).  The writer and
FRESTORE parser cover all 25 longwords at WinUAE's offsets; FRESTORE
of a $60 frame re-arms the pend exactly like the e1 tier (delivery
pre-instruction, vectored from the restored enables).  FSAVE no
longer traps for ANY pending exception class -- the last documented
wart of the FPU exception model.  Directed coverage: t_fpu 318-322
(no trap at FSAVE, $4160 frame id, E3 flags, WBTEMP's exact internal
exponent for 2^248 squared under single rounding precision, FRESTORE
re-arm delivery); negative control (BUSY path disabled) fails 319 in
every phase.

X2.1b BUILD TRIAL (2026-08-17).  The DUAL variant (MISTER_DUAL_SDRAM=1
+ sys_dual_sdram.tcl in a scratch copy) compiles and fits end to end:
36,920 ALMs (88%), pins bind, the probe and fault gating elaborate.
Two findings: (a) the init probe's first fit put its compares in the
hot sdata_reg/sdata2_reg fanout cones (-0.5 ns on clk_114) -- fixed
with dedicated shadow captures and a staged compare, bench green in
all modes; (b) after that, clk_114 still misses by -0.307 concentrated
ENTIRELY in sdata2_reg -> cfill_dat, the dual-read fill rotation mux
(the margin the original audit's 2.5 flagged).  Deploy debt PAID (2026-08-18): the fill path got its own capture-
register copies and a private bypass/rotation mux (identical selects,
same data by construction; the laneswap break force covers both
copies), letting the fitter place the fill cone at cpu_cache_new's
port.  The DUAL variant's clk_114 now MEETS (+0.101); the residue is
sub-0.2 ns single-path seed noise (clk_sys -0.189, hdmi -0.077) --
no structural debt, one seed pick at deploy time.  The DEFAULT build
is unaffected (fits at 37,197 / 89%, functional clocks met, SEED 14
in qsf).

5.1/5.2/5.3 FIXED (2026-08-17) -- the latent internal-cache snoop
bugs, cleared ahead of the X2.7 re-enable.  New directed bench
tb_ap040_cache_snoop.v (a run_tests.sh leg) drives ap040_cache
directly with ce-independent snoop pulses, scripted clkena freezes and
swept snoop timing; pre-fix it failed T1 (5.1: snoop in a frozen-ce
window vanished), T2 at the tag-writeback offset (5.2a: the fill
revalidated the snooped line) and T4 twice (5.3: a line-crossing
store's second-set invalidate lost to a displaced replay).  Fixes:
snoop invalidates are now FREE-RUNNING on port B (the ce-gated write
was the 5.1 loss; the only suppression is a same-row sweep write of
the same zero value); a snoop hitting an in-flight fill's row poisons
the fill (data still delivered, line not validated -- also removes the
same-cycle write/write against C_TAGW); a snoop colliding with a
lookup's acceptance/compare forces a miss (the sim-invisible
mixed-port DONT_CARE half is thereby covered by construction, noted in
the bench); winv_pend now clears only when port B actually served it,
C_WINV retries until it does, the displaced-invalidate slot records
only the C_IDLE first-set case, and a new store stalls one cycle
behind a pending replay so the single slot cannot be overwritten.
T3's concurrent-read expectation is deliberately relaxed to
either-value (a same-cycle snoop is unordered against the read); its
assertion is the invalidate's survival.  t_cache's architected
stale-until-CINVA expectations are unaffected (the tb pokes memory
without snoops there, by design).

RTE DID NOT SAMPLE INTERRUPTS (2026-08-20, from hardware) -- the actual
cause of the irq/all failure, and a case where one of our own tests had
enshrined the bug.

cputest enters every test through RTE, which restores SR and redirects
in one step.  ap040_core's S_RTE_FIN2 went straight to S_FETCH and
sampled interrupts nowhere -- unlike fetch_next and go_pc, which both
do.  A request the restored mask unblocks was therefore missed at that
boundary: the target instruction ran and the interrupt arrived one
instruction late, stacking $43900004 where the reference expects
$43900000.  The qualification also has to use the RESTORED mask, since
the sr register still holds the pre-RTE value in that cycle -- the same
stale-mask shape as finding 1.1.

Why it was written that way: test 90/91 asserted the deferral outright,
citing gencpu's `ipl_fetched = 10` for i_RTE.  That marking feeds only
the cycle-exact 68000/68020 paths (check_ipl/check_ipl_next are emitted
under using_ce and isce020()); the 68040 model has no deferral and
checks interrupts in do_specialties once RTE has restored SR and PC.
The same comment also cited "hardware cputest irq/all shows the tested
instruction completing first", which was AP040's own failure being read
as the expected result.  The corpus is explicit the other way: the
ANDSR.B round EXPECTS frame PC $43900000.  Test 90/91 and its rationale
are corrected.

Directed coverage: t_exceptions 151 builds a frame and RTEs into the
target with a settled request, requiring the interrupt AT that boundary
with the target instruction not yet executed; it fails on pre-fix RTL.
151 exists alongside 150 (the MOVE-to-SR boundary) because 150 passed
throughout -- that path always sampled, which is exactly why the first
attempt at this test missed the bug.

cputest v24 IRQ 115/115, v20 gate 6/6, full suite green.

IPL RECOGNITION LATENCY (2026-08-20, from hardware).  cputest irq/all
on the board reported an exception-25 frame stacking $43900004 where the
reference expects $43900000 -- the interrupt taken AFTER `andi.b
#0,ccr` instead of before it.  This retracts an earlier call in this
audit: the same signature in the replay harness was written off as a
mid-stream-start artifact, and the board shows it is not.

What is NOT wrong: the boundary logic.  t_exceptions 150 asserts a
request while masked, lets it settle, drops the mask, and requires the
interrupt at THAT boundary with the following instruction not yet
executed.  AP040 passes -- so a settled request is taken at the right
place.

What was wrong: recognition took three cycles.  ipl -> ipl_s1 -> ipl_s2
are the synchroniser, and irq_lvl was a THIRD register stage fed from
them.  cputest times its request (INTERRUPT_CYCLES off SERPER) to land
just before a boundary, and the third cycle pushed it past.  The level
is now taken combinationally from the two synchroniser flops
(irq_lvl_live) whenever they agree, so recognition costs two cycles
instead of three.  The coherence check is unchanged and still guards
against reading a transient encoder value -- 3 -> 5 passing through 7
would otherwise look like an NMI -- and no asynchronous pin enters a
comparison.

Caveat worth keeping: irq/all is calibrated in CYCLES, so it is
sensitive to instruction timing as well as to IPL latency.  Two cycles
is the floor for a synchronised input; if the board still misplaces
this frame, the remaining difference is execution timing rather than
recognition, and chasing it further would mean matching 68040 cycle
counts.

HARNESS: TRACE-RESULT ROUNDS MUST FREEZE AT THE TRACE (2026-08-20).
tb_dat_replay treated a vector-9 entry as the round's RESULT only for
the standalone-trace encoding (e_trace == 2 && e_exc == 9).  But when
T1 is set the tested instruction traces and the corpus records
exception 9 with NO separate trace record (e_trace == 0), so the bench
took the "stacked trace" path instead: it let the synthetic handler
RTE on into the ILLEGAL that terminates every round, and that vector 4
displaced the captured result.  Since cap_sp is read at round END, the
frame comparison then read the terminator's frame off a reused stack
as well.  The condition is now simply e_exc == 9.

This was mis-reporting the integer corpus wholesale, and it is why the
Basic/Default groups had never produced a usable number: the FPU
groups rarely set T1, so the FPU sweeps were unaffected and the defect
stayed hidden.  Basic goes 41/658 -> 597/658 (617 failures -> 61).

AP040 was correct throughout -- instrumenting the core showed it
taking the T1 trace, running the synthetic handler and returning.  The
lesson is the same one the T0 finding taught from the other side: when
the bench and the CPU disagree, instrument before assuming.

FULL v24 CORPUS, first trustworthy measurement (2026-08-20):
  3572 / 3801 pass  (was 2504 / 3801 before the round-termination fix)
  Default 512 failures -> 0.  Residual 229, by cause:
    ~175  IRQ (115) + Basic (60): the mid-stream-start interrupt
          artifact below -- the corpus expects the interrupt taken
          BEFORE the tested instruction, the harness starts out of
          reset with no preceding boundary to sample at.  Signature is
          always the same: the instruction completed (A7 pushed) and
          the interrupt was taken after it.
      47  ODD_EXC (33) + ODD_IRQ (8) + AE (6) exception-frame cases,
          unexamined but the same neighbourhood
       4  BasicFPU -- the corpus-data artifacts settled above
       2  ODD_STK
  So the residue is dominated by one understood harness limitation and
  contains no identified AP040 defect.

Remaining in Basic after the fix, both understood:
  60  BSR.B and friends where the round's expected result is an
      INTERRUPT (iexc 25, ilvl 1).  The corpus expects it taken BEFORE
      the tested instruction, because in the native runner the IPL is
      asserted while a previous instruction is still executing.  The
      replay harness starts mid-stream straight out of reset, and
      AP040 samples IRQs at instruction boundaries (fetch_next), so it
      executes the first instruction and takes the interrupt after.
      A mid-stream-start artifact, not a defect: on hardware there is
      always a preceding boundary.
   1  MOVEC2 trace-SR CCR difference ($6004 vs $6000), unexamined.

T0 TRACE DOES NOT SURVIVE AN EXCEPTION (2026-08-19) -- found from
HARDWARE, and the most consequential fix here: it was breaking cputest
on the board across whole groups, integer and FP alike.

Symptom the user photographed, twice: `cputest basic/all` on `abcd.b
d0,d0` and `cputest fbasic/all` on `fabs.b d0,fp1`, both reporting
  Got unexpected trace exception: SR=2000 PC=403db3xx.
  Exception 4 also pending.
with T0=1 in the test's SR.  The stacked SR of 2000 (T bits clear) is
the tell: the trace was being delivered at the HANDLER's entry, after
exception processing had cleared T -- i.e. AP040's "surviving T0
trace", firing inside cputest's own code after the ILLEGAL that
terminates every test.  Every test in every group ends that way, so
this was mis-reporting the whole corpus on hardware while the sim
replay never saw it.

AP040 armed texc_pend for illegal/privilege/A-line/F-line, reading
WinUAE's Exception_cpu_oldpc as the path every exception takes.  It is
not.  gencpu emits exception_cpu() for exactly five things --
divide-by-zero, CHK, TRAPV, TRAP #n and the RTE format error -- and
Exception_cpu_oldpc then does `if (cpu_model >= 68040 &&
internalexception(nr)) t0 = false`, which covers precisely those
vectors (5, 6, 7, 32-47), with nr == 14 excluded separately.  So on a
68040 activate_trace() is unreachable.  Everything else, op_illg's
Exception(4) included, runs through plain Exception() ->
exception_check_trace(), which ends `regs.t1 = regs.t0 = 0` and only
re-arms for T1 below 68040.  Conclusion: no exception leaves a T0
trace pending on the 040.  The arming is removed.

The texc machinery itself is retained -- it still delivers a trace
that a simultaneous INTERRUPT preempted (fetch_next and go_pc), which
is a different and real case with its own coverage.

Test impact, again cutting against our own work: t_exceptions 109
asserted the survival outright ("one survivor trace + one from the SR
write") and was written from the same misreading.  It now requires the
SR write's trace alone.  The neighbouring CHK case is unchanged --
internal exceptions cancelled the trace before and still do, for a
different reason than the comment claimed.

This is a reminder worth keeping: the sim corpus replay agreed with
AP040 here for months, and only the board disagreed.  Hardware outranks
both the harness and our reading of the oracle.

FPSP ROUTE PRECEDES EA REJECTION (2026-08-19) -- the last of the v24
exception-model findings, and the one that cleared the bulk of them.

On the 040 an FPSP-emulated OPMODE with an illegal EA is still reported
through the unimplemented-INSTRUCTION route (vector 11, format $2, PC
of the following instruction).  AP040 rejected the EA first and emitted
a plain format-$0 F-line.  WinUAE's get_fp_value runs
fault_if_unimplemented_680x0 BEFORE it returns 0 for a Dn or An source,
so `fint.d d0,fp1` takes the FPSP frame while `fabs.d d0,fp1` -- same
malformed EA, hardware opmode -- stays a format-$0 F-line.  AP040 now
carries fp_op_in_hw (mirroring ap040_fpu's op_in_hw) and picks the
route accordingly.  One exception preserved from the source: a PACKED
Dn source returns 0 outright on the 040 without consulting the opmode,
so it stays an F-line either way.

Three consequences fixed with it:
  * the (An)+ / -(An) update stands on the unimplemented-INSTRUCTION
    route too, not just the datatype route (same get_fp_value ordering)
  * a store whose destination cannot hold the format records no FPIAR,
    like the An and PC-relative destinations before it
  * the packed-STORE path leaves (An)+ / -(An) updated as well

Directed coverage: t_fpu 326 (`fint.x a1,fp1` must take the format-$2
route where FABS with the same EA does not), 327/328 (rejected store
destination leaves FPIAR at -1), 329 (packed store through (An)+
advances the register by 12).  326 fails on pre-fix RTL.

SCOREBOARD after the v24 alignment (2026-08-19):
  v24 FPU groups   451/562  ->  558/562   (4 failures left)
  v20 FPU groups   329/365  ->  237/365
  v20 gate 6/6, full RTL suite green throughout.
The two move in opposite directions because every fix resolves a case
where v20 and current WinUAE disagree.  Convergence on v24 to within 4
slices -- and those four are arithmetic, not exception-model -- is the
evidence that the direction is right: a wrong change scatters failures
rather than collapsing them.

REMAINING v24 (4 slices) -- the first v24 findings that are NOT about
how an exception is reported, and the next thing to look at:
  BasicFPU/FADD.L    401c ec8f0f87 2a91fcac  vs  401d c4e82b87 9548fe56
  BasicFPU/FNEG.B    c004 f0000000           vs  c005 82000000
  BasicFPU/FSNEG.S   a5401d00                vs  ec401d00
  BasicFPU/FSNEG.X   c1700000                vs  00000000
THIRD PASS, SETTLED WITH HARDWARE (2026-08-20): these four are NOT FPU
bugs.  Three independent lines agree.

(1) An independent arithmetic oracle clears the FPU.  diff/run_fpdiff.sh
    against qemu-system-m68k -cpu m68040: 8 seeds in the default mode
    (extended precision, every rounding mode, every instruction) and 12
    seeds in FPDIFF_MODE=arith (all precisions, arithmetic only) -- on
    the order of 1400 generated FP instructions -- ALL MATCH.  The only
    diverging mode, FPDIFF_MODE=unfl, is documented in gen_fpdiff.py
    itself as expected-divergent: AP040 traps denormal operands to
    vector 55 as 040 silicon does, while qemu computes them.

(2) The user's FADD.L hardware capture shows cputest computing the SAME
    source address AP040 uses.  `fadd.l (-$38,a6,d6.w*4),fp5` with
    A6 = $438fff00 and D6 = $00202020 gives $43907f48, and cputest
    prints exactly that ("S 43907f48").  Its own dump there reads
    00.00.00.00, and adding zero leaves FP5 at $401d-c4e82b879548fe56 --
    byte-identical to the "got" value and to FP5 before the
    instruction.  AP040 read the right address and did the right
    arithmetic on what was there.

(3) The operand the reference used is absent from the shipped data.
    Decoding round 6162 (test 114) shows srcaddr = $43907f48 while the
    round's only setup write targets $438f7f48 -- exactly 64KB lower,
    encoded literally as CT_MEMWRITE/CT_ABSOLUTE_LONG "5e 43 8f 7f 48".
    Working the expectation backwards, FP5 goes 1651774915.79 ->
    992199649.79, so the addend must be exactly -659575266 = $d8afae1e;
    that longword occurs NOWHERE in tmem.dat.  FNEG.B is the same
    shape: the expectation needs source byte $3c, the image holds $41
    at the EA ($438003fc, which is A7-2 per WinUAE's own sz2 table).

    So no CPU running against this data can satisfy these rounds -- a
    real 68040 would return FP5 unchanged too.  This is the FABS.X
    artifact family again, reproduced on v24 and corroborated on
    hardware rather than argued from sim.

Superseded first analysis, kept for the record:  Worked FSNEG.X (BasicFPU/0007, round 585) to the
bottom: op f236 495a a2bf is `fsneg.x (d8,A6,A2.w*2),fp2` with A6 =
$438ffefa, A2 = $00007ff0 and an 8-bit displacement of -65, so the EA
is $4390fe99 -- exactly what AP040 computed, brief-format decode and
all (the scale field of 1 means x2, which is easy to misread as x1).
The operand living there has a biased exponent of $6a28, so negating
it and rounding to SINGLE overflows, and AP040's infinity with the I
bit in FPSR is the correct answer FOR THAT OPERAND.  The corpus,
however, plants this round's 12-byte operand at $438ffe99 -- exactly
$10000 lower, which is opcode_memory - 359, i.e. a RELATIVE_START_WORD
address -- and that address is only reachable if A2's low word were
$fff0 rather than $7ff0.  So the disagreement is over where the
operand IS, not over the arithmetic done on it.  FNEG.B shows the same
shape from the other direction: `fneg.b -(a7),fp1` with A7 =
$438003fe reads $438003fc ($41, giving -65) per WinUAE's sz2 table,
the reference answer is -60, and the corpus's own planted byte at
$438003fd is $6d -- matching NEITHER, so that round's operand is not
where the harness put it either.  Both point at operand
materialization / round association in tb_dat_replay rather than at
ap040_fpu.  Fix the bench first; re-measure before touching the FPU.

First-pass analysis, recorded so the next session does not redo it:
these are whole-value differences, not rounding.  FADD.L comes out an
exponent HIGH with a mantissa ~1.66x the reference, which reads as a
different SOURCE operand rather than a different sum -- its EA is a
full-format extension word (f236 42a2 64c8), so complex-EA resolution
is the first suspect and it would put this in the same family as the
memory-indirect cases.  FNEG.B is `fneg.b -(a7),fp1` with A7 =
$438003fe: AP040 reads $438003fc (A7-2, per WinUAE's sz2 table) and
gets $41 -> -65, while the reference answer is -60; the corpus's own
setup writes $6d at $438003fd, which matches NEITHER answer, so the
operand this round actually consumes has not been identified yet and
the A7 byte-adjust theory does not by itself explain it.  FSNEG.X is
the most suggestive of an AP040 bug: the result mantissa is ZERO where
a value is expected.

FPIAR ON REJECTED FMOVE-OUT DESTINATIONS (2026-08-19) -- third v24
finding.  An FMOVE FPn,<ea> whose destination is An, non-alterable or
PC-relative is an F-line with NO FPIAR side effect.  AP040 wrote FPIAR
on both paths, with comments claiming the command word is "recognized
before destination-EA validation".  The oracle says otherwise and is
unambiguous: for opclass 011 WinUAE reaches maybe_set_fpiar only AFTER
put_fp_value succeeds, and an An or PC-relative destination returns
through fpu_noinst first.  The architecture agrees -- an unrecognized
FMOVE encoding is an F-line, and F-line instructions never reach the
FPU to record an instruction address.  Directed coverage: t_fpu
324/325 (`fmove.p fp0,a0` must leave FPIAR at -1); 325 fails on
pre-fix RTL.  v24 FPU groups 516/562 -> 524/562.

CUMULATIVE v20 DIVERGENCE (2026-08-19), stated together because it is
now large enough to be a decision rather than a detail.  Aligning AP040
to the v24 corpus has moved the v20 FPU sweep from its long-standing
36-slice baseline to 97:
  +49  (An)+ update across a datatype fault
  +12  FPIAR on rejected FMOVE-out destinations
Every one of these is a case where v20 and current WinUAE disagree and
AP040 now follows current WinUAE; none is a case where the two agree
and AP040 dissents.  v20 was generated in 2020 and is the same corpus
that produced the FABS.X artifact this project chased for sessions.
The working assumption from here is that v20's FPU expectations are
stale wherever they conflict with v24, and v24 is the acceptance data.
If that assumption is ever overturned -- by hardware, say -- these
three fixes are the ones to revisit, and each ships with a directed
test that pins the behaviour either way.

(An)+ UPDATE ACROSS A DATATYPE FAULT (2026-08-19) -- second real bug
from the v24 corpus, plus a harness bug that had been hiding it.

RTL: a datatype fault is post-instruction, so the (An)+ / -(An) address
register update STANDS.  AP040's S_FPU_GO unsupp branch performed no
writeback at all, leaving the handler pointed at the operand it had
already consumed.  It now does the same rfw the accepted/done branches
do.  Oracle: WinUAE get_fp_value applies the increment when the EA is
computed (m68k_areg(regs, reg) += sz1[size], 12 for packed) and the
mmufixup undo that could take it back is guarded 68060-only, so on a
68040 it survives the fault.  Directed coverage: t_fpu 323 requires
A3 to advance 12 across a faulting `fmove.x (a3)+,fp1`; it fails in
every phase on pre-fix RTL.

HARNESS: tb_dat_replay sampled the integer register file ONE raw cycle
after S_EXC0 entry.  A write issued by the faulting state has rf_we
high THROUGH that cycle and only reaches the register file on the edge
that ends it, and the sample also has to land on a qualified edge
because the file only commits when ce is high.  The bench therefore
read the pre-write value and reported an architecturally committed
update as missing.  Now deferred two QUALIFIED cycles.  Verified
neutral on its own: with the RTL change reverted and this one kept,
the v20 FPU sweep is back to exactly its 36-slice baseline.

CORPUS DIVERGENCE, stated plainly because it moves a baseline: v20 and
v24 disagree here.  v24 expects the increment (and AP040 now matches);
v20 expects it withheld, so 49 additional v20 packed slices fail and
that sweep goes 36 -> 85.  The current WinUAE source agrees with v24,
and v20 is the corpus already documented as carrying stale-generator
artifacts (the FABS.X case it also got wrong).  v24 is the primary
corpus from here; v20 remains useful only as a frozen comparison, and
its packed expectations are now known-divergent rather than
authoritative.

Result: v24 FPU groups 494/562 -> 516/562.  Full RTL suite green.

DATATYPE-FAULT STACKED PC FIXED (2026-08-18) -- the first real CPU bug
the v24 corpus found, and it had been invisible by construction.

An unsupported/unimplemented FLOATING-POINT DATA TYPE (packed decimal
in any position, or a denormal/unnormal source) is a POST-instruction
exception: format $3 carrying the FOLLOWING instruction's PC.  AP040
stacked the FAULTING instruction's own PC for source operands
(go_fp_unsupp's non-store branch used pc_i), and pc-2 for a statically
unsupported packed store.  Both are now `pc`.

Evidence, in the order it was weighed: the v24 corpus rejects the old
values outright (it decodes the stacked PC as an oracle where v20
rebuilt those four bytes from the processor's own PC and masked them
out of every comparison -- which is exactly why nothing had
contradicted AP040 before); WinUAE agrees explicitly, fpp.cpp taking
the datatype pend through "simplification: always mid/post-instruction
exception" into newcpu_common.cpp, where vectors 48-55 stack currpc
with format $3 when not pre.  Re-verified against the user's
2026-08-18 upstream WinUAE update: DATA_VERSION, the opcode-tail
phase, the restore_bytes escape, that fpp.cpp path and the 48-55 frame
build are all unchanged.

Note the MC68040UM (9.6.2) reads differently -- it calls opclass
000/010 datatype faults pre-instruction with a format $0 frame.  The
corpus and the emulator agree on format $3 with the following PC, and
AP040 already emitted format $3 (the frame word matched in every
mismatch, only the PC byte differed), so the manual is the outlier
here and the acceptance data wins, per this project's standing rule.

Test impact, recorded because it cuts against our own work: five
t_fpu expectations asserted the OLD PC (tests 76, 81, 236, 239, 255,
plus the section comment).  They were written when the corpus could
not check that field, so they encoded AP040's behaviour rather than
the architecture's.  Updated to the following-instruction PC.

Result: v24 FPU groups 451/562 -> 494/562 (43 slices recovered);
v20 representative gate 6/6; full RTL suite green.

REMAINING v24 FPU failures, 68 slices in five classes, all newly
checkable rather than regressions:
  34  exception frame -- e.g. FMOVE.P through memory-indirect EAs,
      frame bytes off by one ($de vs $dd)
  22  A register -- a datatype fault on an (An)+ source leaves the
      address register unincremented; post-instruction semantics say
      the update stands (expected 438fff0c, got 438fff00 for a
      12-byte packed operand)
  14  FPIAR -- AP040 writes FPIAR where the corpus expects it
      untouched (expected ffffffff, got the instruction address)
   4  FP mantissa -- a genuine arithmetic difference (FADD.L family)
   1  exception -- AP040 raises vector 55 where none is expected

CPUTEST v24 CORPUS ADOPTED (2026-08-18, user-supplied data040.zip).
The harness now reads both corpora, and the long-running "phantom
failure" question is settled by data: FABS.X ([0]),fp3 -- the exact
case photographed failing on hardware, and the one this project chased
across multiple sessions -- PASSES 4/4 on v24.  AP040 was right; the
v20 data was stale.

Porting was four format changes, each verified against WinUAE
cputest sources rather than inferred from the bytes:
  * container: v24 ships one .daz per instruction -- ("\xafMRG", u32
    length, gzip member) records, header first, any non-magic word
    ending the chain (main.c load_file_offset).  Unpacked once into a
    hash-keyed cache under build/cputest/unpacked.
  * header: v24 inserts three longwords after the lvl/mask word, so
    fpu_model and everything after it shift up three and the name moves
    from +80 to +92 (main.c header reader).  Both versions parse.
  * restore_bytes: the escape is keyed on a whole $FF byte followed by
    FULL offset and length bytes, where v20 keyed on the packed length
    field being 31 and carried no offset byte.  Reading it the v20 way
    shifts the opcode image and the CPU executes the fill pattern.
  * exception frame: v24 encodes the STACKED PC as a rel_ordered oracle
    ahead of the frame word.  v20 rebuilt those four bytes from the
    processor's own PC and masked them out of the comparison -- so the
    stacked PC was never checked before and now is.

Two harness bugs fell out of that last point, both invisible under v20:
  * setup patches were emitted grouped by kind (all memory writes, then
    all opcode bytes) instead of in stream order.  v24 setups fill the
    opcode area with $F2 through a CT_PC_BYTES record and then write the
    instruction over it, so grouping let the fill erase the instruction.
  * the NOP/ILLEGAL opcode tail was toggled from the wrong starting
    phase.  The phase is genuinely version-dependent (measured both
    ways): v20 data wants ILLG:NOP, v24 wants NOP:ILLG as current
    cputest does, and forcing either onto the other corpus fails --
    nine v20 slices (FBcc, FDBcc, FMOVEM.X, FSMUL.W) break under the
    v24 phase.  Gated on the header version.

Regression status: the v20 full FPU sweep still fails exactly the
documented 36 slices, fail-set identical; v20 representative gate 6/6;
RTL suite green.

NEW SIGNAL from v24, not yet dispositioned: the FPU groups run
451/562, and 107 of the 111 failures are the whole PackedFPU group,
all with the same signature -- an unimplemented-data-type exception on
a packed operand (e.g. fabs.p (a6),fp1) stacks the FAULTING
instruction's PC where the corpus expects the NEXT instruction's.
WinUAE decides this with regs.fpu_exp_pre (newcpu_common.cpp
Exception_build_stack_frame_common: vectors 48-55 stack
instruction_pc with a format $0 frame when pre, currpc with format $3
when post), so the question is whether AP040 classifies packed
unimplemented data types as pre- or post-instruction.  The remaining
four are BasicFPU FADD.L, FNEG.B, FSNEG.S and FSNEG.X.  These are
newly-checkable behaviour, not regressions -- v20 could not see them.

CACHE RE-ENABLE TRIAL (2026-08-18, user-directed: "I need the 68040
cache implemented as BRAM if there's enough left").  Answer: there is
enough AREA and enough BRAM, and there is NOT enough timing slack --
and the blocker is not the cache.

  Area, with AP040_ENABLE_CACHE=1: the design fits at 37,940 / 41,910
  ALMs (91%) and 250 / 553 RAM blocks (45%).  The entity table prices
  ap040_cache at 283 ALMs and +13 M10K: its storage really is block RAM
  by construction (explicit dpram tag row carrying tags+valid+
  round-robin, four inferred cdata ways), so enabling it buys 4KB+4KB
  of cache for a third of a percent of the device.  The 2019-era
  "4226 LABs against 4191" note in cpu_wrapper.v was measured with the
  ATC still in flops and is obsolete.

  Timing: the CPU clock (clk_28, counter[1]) goes from +0.879 ns to
  -4.988 ns (TNS -11.4).  The worst path does NOT traverse the cache:
  it runs ATC dpram -> ap040_mmu -> ap040_core exc_fmt[0], 65 logic
  levels with 59% of the delay in routing.  That cone closes with 2.5%
  of its period to spare in the shipping build, so 283 ALMs of added
  congestion is enough to break it.  Quartus is already at maximum
  effort (HIGH PERFORMANCE EFFORT, PHYSICAL_SYNTHESIS_EFFORT EXTRA,
  register retiming, combo logic), so no build-setting lever remains.

  Value, measured with tb_prof on tests/ap040/asm/bench_loop.s (a new
  loop-and-array benchmark: short inner loop, 256-byte working set,
  self-checking):

                        cache off     cache on
    zero-latency bus     584,894      414,834    1.41x faster
    latent bus           942,404      415,618    2.27x faster

  The second row is the real result: with the internal cache the CPU is
  almost immune to bus latency (+0.2% from adding it), against +61%
  without.  The old blanket claim that ap040_cache "ran SLOWER" holds
  only for straight-line miss-heavy code, which is what the regression
  programs are -- t_integer is 1.36x slower with it on (25,410 vs
  18,742).  Real code spends its time in loops.

  Root cause, then fix (same day).  The cone was read out of TimeQuest
  node by node rather than guessed at: ATC block RAM -> mmu|hit0 ->
  mmu|atc_fault -> cache|pass_active -> core -> 47 levels of
  exc_fmt[2]~NNN priority mux.  ap040_cache was forwarding a BYPASSED
  access straight through in C_IDLE -- pass_active included
  "cst == C_IDLE && c_req", and c_ack is "pass_active ? m_ack : ack_r",
  so c_req (which carries the ATC compare) reached the core's mem_ack
  combinationally, and mem_ack gates 48 exc() sites.  ap040_mmu already
  refuses precisely this shortcut, and says so in its own c_ack
  comment; the cache now follows the same rule: pass_active is
  (cst == C_PASS) alone, and a bypassed access is issued from C_PASS.

  Result with AP040_ENABLE_CACHE=1 and that one change: the design is
  timing-CLEAN for the first time this session.  clk_28 +0.844 (from
  -4.988), clk_114 +0.132 -- which also retires the pre-existing
  -0.145 violation in the legacy cpu_cache_new dtram -> cpu_ack path --
  pll_hdmi +0.093, hold +0.246, recovery +3.359, and no
  "Timing requirements not met" warning at all.  37,962 ALMs (91%),
  250 RAM blocks (45%).

  Cost of the extra cycle: bypassed accesses only (I/O, misaligned,
  cache-inhibited); cacheable traffic goes through C_LOOK untouched.
  Measured at 0.02% on bench_loop (414,904 vs 414,834 cycles) and
  0.35% on t_integer (25,500 vs 25,410).  Full suite green, cache
  snoop bench T1-T5 green, corpus gate 6/6.

5.4 FIXED (2026-08-18) -- the last blocker to re-enabling the
internal caches.  ap040_cache had no bus-error input at all: on a fill
beat that faulted, the adapter dropped the cycle without m_ack and the
cache sat in C_FILL re-issuing the same address forever while the core
had already taken the fault and moved on -- livelock, not a format $7.
The cache now takes m_err (wired to the same berr the core and the
adapter see) and abandons the transfer: C_FILL drops r_issued and
returns to C_IDLE WITHOUT reaching C_TAGW, so the partly-filled line is
never validated and its beats are unreachable; C_PASS releases the bus
the same way but still honours an owed second-set invalidate (more
invalidation is always safe under write-through); and a new err_hold
flag refuses to re-accept the still-level-held request until the core
withdraws it, which would otherwise re-issue the faulting address on
the next cycle.  Directed coverage: tb_ap040_cache_snoop T5 sweeps the
error across all four fill beats plus a faulting write-through store,
requiring the bus to go idle, the cache to keep serving (the exception
handler's own accesses), and the abandoned line to refetch changed
memory.  Pre-fix (port present, abort logic absent) the bench wedges:
CINV never completes and reads return the abandoned fill's data --
12 failures against 0 after.

AREA (2026-08-17, user-directed: "the cache should be BRAM").  The
first full compile of the working tree FAILED to fit: 42,067 ALMs
against 41,910 (committed HEAD baseline: 39,645 / 95%, so the X2.2
queue plus the fix program added ~2.4K).  The entity table pinned the
sink: ap040_mmu at 6,077 ALUTs + 5,772 registers with ZERO block-RAM
bits -- the 128-entry ATC payload in flops.  Fixed by converting the
ATC to a 180x32 bram.vhd dpram (one row per {bank, set}, 4 ways x 45
bits), the explicit-instantiation lesson from P1: valids/round-robin
stay in flops (single-cycle PFLUSHA, warm-reset retention), a
free-running one-clock lookup pipe with a live-request guard covers
enabled translation only (TC.E=0 and TTR paths stay combinational and
free), pass_ok explicitly refuses to forward an enabled non-TTR access
on a stale pipe, fills compose read-modify-write rows from the port-B
read held during the walk and write in the commit cycle (a registered
strobe would race the round-robin update), and PFLUSH page/nonglobal
plus the PTEST pre-flush became a ~34-cycle sweep (PFLUSH is rare; the
core is stalled behind it anyway).  tb_ap040_double_fault's
warm-reset ATC-retention test now plants/checks the dpram row slice.
Result: 37,170 ALMs (89%), clk_114 +0.266 / clk_sys +0.937 (pll_hdmi
-0.323, display-only, seed-sweep before any RBF), full suite green,
representative corpus gate 6/6.  This also completes X2.7's "MMU
pruning" line ahead of schedule, as storage conversion instead of
feature removal.

3.2 MITIGATED (2026-08-17).  The queue-vs-store snoop is in: a
completing CPU write inside the fetch queue's [epf_next, epf_ftail)
window flushes it, so rewritten words are refetched.  Stronger than
the architected CPUSH/CINV requirement and deliberate -- the previous
fetch-buffered core booted DiagROM but not AmigaOS on hardware with
exactly this stale-prefetch hazard.  Directed coverage: t_integer 192
rewrites the next instruction while it sits in the queue and requires
the NEW instruction to execute; the test bypasses the internal I-cache
(CACR IE off) for its window because a cached stale line is t_cache's
architected stale-until-CINVA contract and a separate layer -- on the
shipping build the internal cache is compiled out, so the queue is the
only instruction staleness there.  Seen failing pre-fix and with the
snoop neutered, in every phase.  The debugging of this test also
CONFIRMED by direct trace that the internal I-cache (when enabled)
serves stale lines across CPU stores, i.e. the audit section 5.5
observation, reachable through the architected contract.  Hardware
validation of X2.2 remains the final closure, but the known
fetchbuffer failure class now has a mechanism standing in its way.

1.7 FIXED (2026-08-17).  The MOVEM restart hole is closed within the
restart model: a loaded register that is also the EA base is no longer
written mid-loop.  For (An)+ the skip is architecturally free (the
68040 leaves the postincremented address, not the memory value); for
control modes the loaded value is held (mm_base_pend/mm_base_val) and
committed with the last transfer, so a fault on any later transfer
restarts with the ORIGINAL base and the EA recomputes correctly.
Directed coverage: t_mmu 144-147 -- movem.l (a0),a0-a2 with a0's
memory image a non-address and the third transfer faulting; seen
failing pre-fix (the garbage-EA restart derails the run) and again
with the deferral neutered.  The test restores cnt_aerr afterwards
(later sections count faults absolutely).

7.3 CLOSED (2026-08-17).  The dead code from the reverted RTE
interrupt-deferral experiment is deleted: rte_irq_pend /
rte_irq_take_lvl (which replicated the pre-1.1 hold logic and invited
re-inheriting that bug) and the never-read rte_oldsr with its three
write sites.  Suite green after removal.

1.8 FIXED (2026-08-17).  Trace-vs-interrupt order resolved against
WinUAE do_specialties: the completing instruction's trace converts to
a PENDING trace and the interrupt is sampled after that conversion, so
the interrupt exception processes FIRST and the trace is delivered at
its handler's entry -- never lost, never first.  Both boundary paths
now agree and use the existing S_EXC_JMP texc machinery: go_pc's tr_t1
branch defers to a pending interrupt (was: trace-first), and
fetch_next's interrupt branch converts a simultaneous trace to
texc_pend instead of dropping the event (was: trace lost, resumed only
at the next instruction).  The corpus-pinned constraint in the old
comment (odd-IRQ-vector nested AE stacks the accepted interrupt mask)
is preserved -- the interrupt still goes first.  Directed coverage:
t_exceptions 142 (change-of-flow: IPL swept into a traced RTS; some
delay must land both at one boundary, where the IRQ frame returns to
the RTS target and the trace frame stacks h_int2's entry -- seen
failing pre-fix at every sweep value) and 143 (straight-line: swept
into a traced divide, same signature -- seen failing with only the
go_pc half fixed, and again with the fetch_next conversion neutered).
Also fixed en passant: fberr_fa's scratch address collided with
trap_guard (latent, masked by test ordering); moved to $3654.

2.3 residue CLOSED (2026-08-17).  tb_sdram32's three deliberate breaks
are now plusarg-selectable (+break_lockstep / +break_laneswap /
+break_chipwr, implemented as forces re-deriving the commit's one-off
edits: 3390/1986/646 errors) and run_tests.sh gained negleg legs that
REQUIRE each break run to fail -- the negative tests are part of the
per-commit gate.  Still open from section 2: the runtime fallback
probe (2.2), deferred to the X2.1b integration package.

6.1 FIXED (2026-08-17).  ap040_walker_cdc's s_ack is now LEVEL-HELD
until the MMU drops s_req, instead of a single s_clk pulse sampled
under ce.  The MMU's request-low re-arm cycle (whose comment in
ap040_mmu.v already anticipated a held ack) guarantees no
double-consumption.  Directed coverage: tb_ap040_walker_cdc's
transact_blind models a ce-gated consumer that goes blind for 40
cycles after issuing -- the pulse died inside the window on the
pre-fix module (seen failing), the held ack survives and clears
within 2 cycles of the request dropping.  This removes the unstated
cross-module invariant that would have hung the walker under X2.2's
decoupled fetch.

6.2 FIXED (2026-08-17).  The module now crosses its own s-side reset
into the m domain (async assert, m_clk-synchronized release), so a
CPU-only reset clears BOTH sides and the toggle parity cannot desync
regardless of top-level reset wiring; Minimig.sv needs no change.
Directed coverage: tb_ap040_walker_cdc drives an s-side-only reset at
odd toggle parity with the m side running -- the pre-fix module
launched a phantom m-side request from the stale payload (seen
failing: phantom request + count), the fixed module stays quiet and
the bridge works after the one-sided reset.

2.1 FIXED -- and the mechanism was worse than audited.  The SDRAM
modules short DQMH/DQML to A12/A11 (the convention sdram_ctrl already
serves at its write CAS with sd_addr[12:11] <= cas_dqm), so with
sd2_addr a pure lockstep copy the secondary chip's effective DQM
received the PRIMARY's mask value 2'b11 during secondary-lane writes:
every 16-bit write to the secondary lane was dropped whole on the real
board (not byte-clobbered as the tied-low theory guessed), while
tb_sdram32 passed by honoring the phantom sd2_dqm pin.  Fix:
sdram32_ctrl drives sd2_addr[12:11] with cas_dqm2 for exactly the
write-CAS hold window (sd2_a_dqm/sd2_a_dqm_live; lockstep-identical
everywhere else); tb_sdram32 now models the module short (secondary
model dqm from addr[12:11], addr[12:11] excluded from the lockstep
identity as lane controls) and FAILED with 548 errors on the pre-fix
RTL under the faithful model, all green after.  tb_sdram32 is also
wired into run_tests.sh now (closes finding 12/2.3's leg gap; the
plusarg break modes and the fallback probe remain open).  Caveat
recorded in the plan: a secondary module with real unshorted DQM
routing would need RMW instead.

NEW pre-existing finding (surfaced by the FMOV* slice run, NOT a
regression): 4 of 23 FMOVE slices fail -- BasicFPU/FMOVE.P 0001+0002
and PackedFPU/FMOVE.P 0001+0002, all as single exception-frame EA
bytes in format-$3 vector-55 frames (e.g. byte 9 at 420007bd expected
d1 got da).  A/B against committed HEAD (scratch worktree, same
corpus): byte-identical failures, so this predates the X2.2 diff and
every fix above.  decode_cputest_dat.py on BasicFPU/FMOVE.P shows the
failing setups are packed k-factor stores whose recorded frames carry
EA=0/fpeaset=0 while the harness expects varying nonzero EA bytes --
the same v20 generator/runtime memory-model divergence family as the
documented FABS.X/FADD.P artifacts, and AP040's v55 store-EA
convention is hardware-adjudicated (E55 frames compare clean on
hardware).  FULL-FAMILY SIZE (2026-08-18): a complete FPU-group sweep (365
slices) surfaces 36 failing slices, all the same memory-indirect/
complex-EA neighbor-dependency class incl. the documented FABS.X
case itself; the fail SET is programmatically identical on committed
HEAD (A/B in a scratch worktree), so the Tier-1 FSAVE work and every
session fix are corpus-neutral.  36 is the expected v20 baseline for
full FPU-group sweeps (recorded in the phantom-failures memory).

CONFIRMED as that artifact family (2026-08-17): every
mismatching op is a packed store through a MEMORY-INDIRECT EA --
f2306e000795 (the identical 0795 extension word as the documented
FADD.P artifact), f2326e809117, f2306d803395, f2316e003515 -- failing
only on v55 frame-EA bytes with small pointer deltas, exactly the
reverted-pointer walk.  Classified expected-fail on v20 data
(recorded in the cputest-v20-phantom-failures memory); regenerating
the corpus with a current WinUAE remains the real remedy.

Plan text corrected per section 4: P1 STATUS paragraph (revert
recorded, snoop-port paragraph), F0 divide/sqrt (3 bits/digits per
cycle as built), X2.1 io-board byte-mask mechanism, X2.2 gate
re-scoped by measurement.

REMAINING OPEN after this program (all design/integration-level):
  (1.5 / finding 6 CLOSED 2026-08-18: Tier 1 sections 1.4b and Tier 2
  section 1.5 above.  Both frame shapes ($41/$30 e1 and $41/$60 e3
  BUSY) extract and re-arm; FSAVE no longer traps for any pend class.
  One accepted simplification, kept from the design note: FRESTORE of
  a $60 frame re-arms trap DELIVERY from the restored enables rather
  than re-executing the arithmetic from ET/FPT (WinUAE's cusavepc=$FE
  continuation) -- equivalent whenever the FPSP-style handler either
  completes the op itself or leaves the frame intact, which is every
  handler observed; wbtm66 stays 0 with a comment.)
  2.2               sdram32 runtime fallback probe (X2.1b package)
  4.6               format $4 generation (needs an AP040_HAS_FPU=0
                    build configuration to exist first)
  (5.5 CLOSED 2026-08-20: the walker half.  ap040_tg68k_compat now
  raises the data cache's snoop on every table-walker WRITE, so the U/M
  bits the walker pokes into descriptors invalidate any line the CPU had
  cached by reading those descriptors as data.  The walker is inside the
  CPU, so no SoC plumbing was needed -- its address is already physical
  and the cache is physically tagged.  The chipset pulse keeps priority
  (it comes from a CDC edge and cannot be held); one pending slot is
  enough because walker writes are whole memory transactions apart.
  Directed coverage: t_mmu 148/149 rewrites page 7's descriptor, flushes
  the ATC, caches the descriptor by reading it, touches the page and
  requires the walker's U bit to be visible; 149 fails with the snoop
  disconnected.  The OTHER half of 5.5 stands as documented: stores and
  snoops invalidate the data bank only, so DMA-loaded CODE still needs
  CINV, which matches Amiga discipline and is weaker than a real
  snooping 040.)
  section 5         latent internal-cache snoop/port-B bugs -- fix
                    with a directed s_stb bench before any re-enable
                    experiment (5.5's stale-I-line-across-stores was
                    trace-confirmed on 2026-08-17)
  7.1 / 7.2         double-fault window breadth and RTE-odd-PC S-bit:
                    UM self-contradictory, corpus-pinned as-is; revisit
                    only with hardware evidence
  9.1 / 9.2         FPU delivery-timing consistency and FRESTORE
                    header-word strictness (documented)
  X2.1b             Minimig.sv/qsf integration of sdram32_ctrl behind
                    DUAL_SDRAM=1 (controller side now unblocked)
  X2.2              hardware validation (SMC insurance in place; the
                    occupancy gate lives with X2.3 now)
